DIAGNOSE AND FIX EMAIL DELIVERY FOR FORGOT PASSWORD + BOOKING EMAILS

Problem:
Forgot Password says success but no email arrives.

Goal:
Find the exact reason emails are not being delivered and fix it safely.

CRITICAL:
Do NOT break login/register/booking/admin/i18n.
Do NOT reset Neon.
Do NOT expose secrets.
Do NOT change unrelated logic.

Tasks:

1. Inspect current email service configuration:
- Which provider is used? Resend?
- Required env variables:
  RESEND_API_KEY
  EMAIL_FROM
  PUBLIC_APP_URL or equivalent production URL

2. Check if the app safely detects missing email config:
- If RESEND_API_KEY is missing, log clearly:
  "Email provider not configured: RESEND_API_KEY missing"
- Do NOT crash server.
- Do NOT expose this to users.

3. Verify Forgot Password email flow:
- POST /api/auth/forgot-password creates token
- email send function is called
- reset link uses production URL:
  https://youssef-booking.vercel.app/reset-password?token=...

4. Verify booking email flow:
- trainer booking email is sent to:
  youssef.physical@gmail.com
- client confirmation email is attempted

5. Add safe admin/debug email test endpoint if not already available:
- admin-only route:
  POST /api/admin/test-email
- Sends test email to youssef.physical@gmail.com
- Returns clean status:
  { ok: true }
  or
  { ok: false, reason: "RESEND_API_KEY missing" }
- Do NOT expose secret values.

6. If env variables are missing:
- Tell me exactly which ones to add in Vercel.
- Do not pretend emails are working.

7. If code issue exists:
- Apply minimal fix only.
- Email failure must not break booking/login.
- But server logs must clearly show the exact failure reason.

8. Required env variables:
- RESEND_API_KEY
- EMAIL_FROM
- PUBLIC_APP_URL=https://youssef-booking.vercel.app

9. Deployment:
- Run TypeScript check
- Commit changes
- Push to GitHub main
- Wait for Vercel auto-deploy
- Confirm production READY

10. Final verification:
- Use the admin test email endpoint
- Trigger Forgot Password
- Confirm whether email is actually sent
- Report:
  Trainer email: PASS/FAIL
  Forgot password email: PASS/FAIL
  Missing Vercel env vars if any

Final result:
Email delivery diagnosis is clear, forgot-password email either works or exact missing setup is reported.